[project]
name = "Terminus"
version = "0.0.1"
authors = [
  { name="Matěj Šindelář", email="102618601+matee28@users.noreply.github.com" },
]

description = "Pygame simulátor železničního magnáta"
readme = "README.md"
requires-python = ">=3.12"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]

license = "GPL-3.0-only"
license-files = [
    "LICENSE",
]

dynamic = ["dependencies"]

[tool.setuptools.dynamic]
dependencies = {file = ["requirements.txt"]}


[project.scripts]
terminus-test = "terminus_test:main"


[project.urls]
Homepage = "https://github.com/matee28/Terminus"
Issues = "https://github.com/matee28/Terminus/issues"


[build-system]
# Minimum requirements for the build system to execute.
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]